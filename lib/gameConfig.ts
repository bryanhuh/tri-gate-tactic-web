/**
 * Shared game configuration constants.
 * Single source of truth for game balance values referenced across the codebase.
 */

/** Maximum hit points for each player at the start of a match. */
export const MAX_PLAYER_HP = 1000;
